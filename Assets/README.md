# Демка на Unity ECS

# Стек

### Основные технологии
- **Unity**
- **LeoECS**
- **Zenject**
- **DOTween**
- **UniLeo**
- **TextMeshPro** 
- **Tetra Attributes** 

## Архитектура проекта

Проект использует ECS архитектуру со следующей структурой:

```
Assets/
├── Scripts/
│   ├── ECS/
│   │   ├── Components/      # Компоненты
│   │   ├── Systems/         # Системы
│   │   ├── Events/          # События
│   │   ├── Tags/            # Теги для фильтрации
│   │   └── References/      # Ссылки на Unity объекты
│   ├── DI/
│   │   ├── Installers/      # Installers
│   │   └── Views/           # Классы для связи Unity и ECS
│   └── Data/                # Данные (SO)
```

### Компоненты

#### `HealthComponentProvider`
- **Health** (float) - текущее здоровье персонажа
- Используется системой `CharacterDamageSystem` для обработки урона
- При Health <= 0 срабатывает `DestroyEvent`

#### `MotionComponentProvider`
- **RelativeDirection** (Vector2) - относительное направление движения
- **LocalDirection** (Vector2) - локальное направление движения
- **Rotation** (float) - угол поворота персонажа
- **Velocity** (float) - вертикальная скорость (для гравитации)
- **Speed** (float) - скорость перемещения персонажа
- Используется `CharacterMotionSystem` для движения персонажа

#### `PlayerInputComponentProvider`
- **AxisDirection** (Vector2) - направление ввода (WASD/стрелки)
- **MousePosition** (Vector3) - позиция курсора мыши
- Используется `PlayerInputSystem` для обработки ввода игрока

#### `WeaponsComponentProvider`
- **Weapons** (List<EcsEntity>) - список всех оружий персонажа
- Используется для управления коллекцией оружий

#### `FootstepComponentProvider`
- **Timing** (float) - тайминг шага
- **Delay** (float) - задержка шага
- **StepDelay** (float) - задержка между шагами
- Используется `CharacterFootstepSystem` для воспроизведения звуков шагов

#### `NearestItemComponentProvider`
- **Buffer** (Collider[]) - буфер для коллайдеров предметов
- **Entity** (EcsEntity) - ссылка на ближайший предмет
- Используется `InventoryFindNearestItemSystem` для поиска предметов

#### `WeaponDataComponentProvider`
- **Data** (WeaponData) - ссылка на ScriptableObject с данными оружия
- Содержит звуки выстрела и другие настройки

#### `WeaponParamsComponentProvider`
- **NextFireTime** (float) - время следующего выстрела
- **StartDamage** (float) - базовый урон оружия
- **StartRate** (float) - скорость стрельбы (выстрелов в секунду)
- **AnimationShootDuration** (float) - длительность анимации выстрела
- **Falloff** (float) - коэффициент падения урона с расстоянием
- Используется `WeaponShootSystem` для расчета урона и задержек

#### `WeaponStateComponentProvider`
- **CurrentState** (State enum) - текущее состояние оружия:
    - `Idle` - простаивает
    - `Equipped` - экипировано
    - `Unequipped` - снято

#### `WeaponRaycastComponentProvider`
- **Target** (GameObject) - цель попадания
- **Start** (Vector3) - точка начала луча
- **End** (Vector3) - точка конца луча
- **Normal** (Vector3) - нормаль поверхности попадания
- **Direction** (Vector3) - направление луча
- **Distance** (float) - расстояние попадания
- **CollisionMask** (LayerMask) - маска слоев для проверки коллизий
- Используется `WeaponRaycastSystem` для расчета траектории выстрела

#### `InventoryComponentProvider`
- **CurrentWeapon** (EcsEntity) - ссылка на текущее активное оружие
- **Weapons** (List<EcsEntity>) - список оружий в инвентаре
- **MaxSlots** (int) - максимальное количество слотов
- Используется всеми системами инвентаря

#### `MouseRaycastComponentProvider`
- **Position** (Vector3) - позиция рейкаста мыши в мировых координатах
- **CollisionLayer** (LayerMask) - маска слоев для рейкаста
- Используется `CameraRaycastSystem` для определения позиции курсора

#### `ChildComponentProvider`
- **Entities** (List<EcsEntity>) - список дочерних сущностей
- Используется `ChildInjectionSystem` для связывания родительских и дочерних сущностей

## 🏷️ Теги ECS

- **`PlayerTag`** - тег игрока
- **`EnemyTag`** - тег врага
- **`PickupableTag`** - тег предмета, который можно подобрать
- **`CurrentWeaponTag`** - тег текущего активного оружия
- **`LeftTag`** - тег левой руки/точки
- **`RightTag`** - тег правой руки/точки
- **`HandTag`** - тег руки
- **`FootTag`** - тег ноги
- **`ItemDisplayTag`** - тег отображения предмета
- **`InactiveTag`** - тег неактивной сущности
- **`MeshTag`** - тег меша

## 🔗 References (Ссылки на Unity объекты)

- **`AnimatorReference`** - ссылка на Animator компонент
- **`AudioSourcesReference`** - ссылка на массив AudioSource
- **`CameraReference`** - ссылка на Camera компонент
- **`ChildReference`** - ссылка на дочерние объекты
- **`ControllerReference`** - ссылка на CharacterController
- **`DisplayReference`** - ссылка на UI элемент отображения
- **`EntityReference`** - ссылка на EntityView
- **`LaserReference`** - ссылка на LineRenderer для лазера
- **`ParticlesReference`** - ссылка на ParticleSystem
- **`TransformReference`** - ссылка на Transform
- **`TwoBoneIKReference`** - ссылка на IK компонент (для рук)
- **`WeaponReferences`** - ссылка на GameObject оружия
- **`WeaponsReference`** - ссылка на контейнер оружий

## ⚡ События (OneFrame)

События автоматически удаляются после одного кадра выполнения:

### События оружия
- **`WeaponShootEvent`** - событие выстрела (без параметров)
- **`WeaponHitEvent`** - событие попадания:
    - `Entity` (EcsEntity) - сущность, в которую попали
    - `Damage` (float) - нанесенный урон

### События инвентаря
- **`InventoryAddEvent`** - событие добавления предмета:
    - `Entity` (EcsEntity) - сущность предмета
- **`InventoryRemoveEvent`** - событие удаления предмета:
    - `Entity` (EcsEntity) - сущность предмета
- **`InventoryChangeSlotEvent`** - событие смены слота:
    - `Slot` (int) - номер слота (1-9)

### События персонажа
- **`TakeDamageEvent`** - событие получения урона:
    - `Damage` (float) - количество урона
- **`DestroyEvent`** - событие уничтожения сущности (без параметров)

### События визуальных эффектов
- **`PlayAudioEvent`** - событие воспроизведения звука:
    - `Clip` (AudioClip) - аудио клип
    - `Position` (Vector3) - позиция звука
    - `Volume` (float) - громкость
    - `Pitch` (float) - тон
    - `MaxDistance` (float) - максимальная дистанция слышимости
- **`PlayParticleEvent`** - событие воспроизведения частиц:
    - `Data` (ParticleView) - ссылка на ParticleView
- **`CreateAndPlayParticleEvent`** - событие создания и воспроизведения частиц
- **`PlayTweenEvent`** - событие воспроизведения DOTween анимации

## 🔧 Системы ECS

### Системы инициализации (IEcsInitSystem)

#### `EntityInjectionSystem`
- **Назначение:** Сохраняет ссылку на Entity в MonoBehaviour для связи с Unity API (например Raycast)
- **Выполняется:** Один раз при старте
- **Фильтры:** Все сущности с компонентами

#### `ChildInjectionSystem`
- **Назначение:** Инжектирует дочерние сущности в родительские
- **Выполняется:** Один раз при старте
- **Фильтры:** Сущности с `ChildComponent`

#### `InventoryInitialSystem`
- **Назначение:** Инициализирует инвентарь (создает список Weapons)
- **Выполняется:** Один раз при старте
- **Фильтры:** `InventoryComponent`

#### `InventoryFindNearestItemSystem`
- **Назначение:** Инициализирует поиск ближайших предметов
- **Выполняется:** Один раз при старте + каждый кадр

#### `InventoryEquipSystem`
- **Назначение:** Активирует оружие в руках по событию
- **Выполняется:** Один раз при старте + каждый кадр

#### `CharacterInjectWeaponSystem`
- **Назначение:** Записывает Entity оружия для обработки инвентарем
- **Выполняется:** Один раз при старте

#### `AudioSystem`
- **Назначение:** Инициализирует аудио систему
- **Выполняется:** Один раз при старте + каждый кадр

### Системы обновления (IEcsRunSystem)

#### Системы камеры

##### `CameraRaycastSystem` / `MouseRaycastSystem`
- **Назначение:** Выполняет рейкаст от камеры к позиции мыши
- **Фильтры:** `CameraReference`, `MouseRaycastComponent`
- **Функционал:** Определяет мировую позицию курсора мыши

#### Системы игрока

##### `PlayerInputSystem`
- **Назначение:** Обрабатывает ввод игрока
- **Фильтры:** `PlayerTag`, `InputComponent`, `CurrentWeaponTag`, `InventoryComponent`
- **Функционал:**
    - Читает WASD для движения
    - Читает позицию мыши
    - Обрабатывает ЛКМ для выстрела
    - Обрабатывает F для подбора предметов
    - Обрабатывает G для выброса
    - Обрабатывает 1-9 для смены слотов

##### `PlayerDirectionSystem`
- **Назначение:** Поворачивает персонажа в направлении мыши
- **Фильтры:** `PlayerTag`, `InputComponent`, `MotionComponent`
- **Функционал:** Вычисляет угол поворота на основе позиции мыши

##### `PlayerRiggingSystem`
- **Назначение:** Управляет IK руками для наведения оружия
- **Фильтры:** `PlayerTag`, `TwoBoneIKReference`, `CurrentWeaponTag`
- **Функционал:** Настраивает IK для наведения оружия на цель

#### Системы персонажа

##### `CharacterMotionSystem`
- **Назначение:** Управляет движением персонажа
- **Фильтры:** `ControllerReference`, `MotionComponent`
- **Функционал:**
    - Применяет гравитацию
    - Перемещает CharacterController
    - Поворачивает персонажа
    - Вычисляет относительное направление движения

##### `CharacterAnimatorSystem`
- **Назначение:** Управляет анимациями персонажа
- **Фильтры:** `AnimatorReference`, `MotionComponent`
- **Функционал:** Устанавливает параметры аниматора (скорость, направление)

##### `CharacterFootstepSystem`
- **Назначение:** Воспроизводит звуки шагов
- **Фильтры:** `FootTag`, `FootstepComponent`, `AnimatorReference`
- **Функционал:** Отслеживает анимацию шагов и воспроизводит звуки

##### `CharacterDamageSystem`
- **Назначение:** Обрабатывает получение урона
- **Фильтры:** `TransformReference`, `HealthComponent`, `TakeDamageEvent`
- **Функционал:**
    - Вычитает урон из здоровья
    - При Health <= 0 создает `DestroyEvent`

##### `CharacterDestroySystem`
- **Назначение:** Уничтожает сущности при получении `DestroyEvent`
- **Фильтры:** `DestroyEvent`
- **Функционал:** Удаляет GameObject из сцены

##### `CharacterInjectWeaponSystem`
- **Назначение:** Привязывает оружие к рукам персонажа
- **Фильтры:** `WeaponsComponent`, `HandTag`, `LeftTag`/`RightTag`
- **Функционал:** Устанавливает оружие в руки при экипировке

#### Системы инвентаря

##### `InventoryFindNearestItemSystem`
- **Назначение:** Находит ближайший предмет для подбора
- **Фильтры:** `PlayerTag`, `NearestItemComponent`, `PickupableTag`
- **Функционал:** Использует Physics.OverlapSphere для поиска предметов

##### `InventoryPickupSystem`
- **Назначение:** Подбирает предмет при нажатии F
- **Фильтры:** `NearestItemComponent`, `InventoryAddEvent`
- **Функционал:** Присваивает найденный предмет в `InventoryAddEvent`

##### `InventoryHandlerSystem`
- **Назначение:** Обрабатывает добавление/удаление предметов из инвентаря
- **Фильтры:** `InventoryComponent`, `InventoryAddEvent`/`InventoryRemoveEvent`
- **Функционал:**
    - Добавляет предметы в список Weapons
    - Удаляет предметы из списка
    - Управляет активацией/деактивацией предметов

##### `InventoryEquipSystem`
- **Назначение:** Экипирует/снимает оружие из слотов
- **Фильтры:** `InventoryComponent`, `WeaponsComponent`, `WeaponReferences`
- **Функционал:**
    - Активирует оружие при смене слота
    - Деактивирует предыдущее оружие
    - Управляет тегом `CurrentWeaponTag`
    - Выбрасывает оружие при нажатии G

##### `InventoryDisplaySystem`
- **Назначение:** Отображает инвентарь в UI
- **Фильтры:** `InventoryComponent`, `ItemDisplayTag`, `DisplayReference`
- **Функционал:** Обновляет UI элементы для отображения слотов инвентаря

#### Системы оружия

##### `WeaponRaycastSystem`
- **Назначение:** Выполняет рейкаст для выстрела
- **Фильтры:** `CurrentWeaponTag`, `WeaponRaycastComponent`
- **Функционал:** Рассчитывает траекторию луча и точку попадания

##### `WeaponShootSystem`
- **Назначение:** Обрабатывает логику выстрела
- **Фильтры:** `CurrentWeaponTag`, `WeaponParamsComponent`, `WeaponRaycastComponent`, `WeaponStateComponent`, `WeaponShootEvent`
- **Функционал:**
    - Проверяет задержку между выстрелами
    - Рассчитывает урон с учетом расстояния (falloff)
    - Создает `WeaponHitEvent` при попадании

##### `WeaponHitSystem`
- **Назначение:** Обрабатывает попадание оружия
- **Фильтры:** `WeaponHitEvent`, `HealthComponent`
- **Функционал:** Создает `TakeDamageEvent` у цели попадания

##### `WeaponLaserSystem`
- **Назначение:** Визуализирует лазерный прицел
- **Фильтры:** `CurrentWeaponTag`, `LaserReference`, `WeaponRaycastComponent`
- **Функционал:** Обновляет LineRenderer для отображения луча

##### `WeaponParticleSystem`
- **Назначение:** Создает частицы при выстреле
- **Фильтры:** `WeaponShootEvent`, `ParticlesReference`, `ImpactData`
- **Функционал:** Воспроизводит частицы в точке выстрела и попадания

##### `WeaponSoundSystem`
- **Назначение:** Воспроизводит звуки выстрела
- **Фильтры:** `WeaponShootEvent`, `WeaponDataComponent`, `AudioSourcesReference`
- **Функционал:** Воспроизводит случайный звук из `WeaponData.ShootSounds`

##### `WeaponAnimatorSystem`
- **Назначение:** Управляет анимацией оружия
- **Фильтры:** `WeaponShootEvent`, `AnimatorReference`, `WeaponParamsComponent`
- **Функционал:** Запускает анимацию выстрела на аниматоре оружия

#### Системы визуальных эффектов

##### `AudioSystem`
- **Назначение:** Воспроизводит аудио события
- **Фильтры:** `PlayAudioEvent`, `AudioSourcesReference`
- **Функционал:** Создает временные AudioSource для воспроизведения звуков

##### `ParticleSystem`
- **Назначение:** Управляет частицами
- **Фильтры:** `PlayParticleEvent`, `CreateAndPlayParticleEvent`, `ParticlesReference`
- **Функционал:** Воспроизводит или создает частицы по событиям

## 📦 Данные (ScriptableObject)

### `WeaponData`
- **Путь создания:** Create > Weapon > WeaponData
- **Поля:**
    - `ShootSounds` (AudioClip[]) - массив звуков выстрела

### `FootstepSoundsData`
- **Путь создания:** Create > Effects > FootstepSoundsData
- **Поля:**
    - `StepData` (Data[]) - массив данных о шагах:
        - `Clips` (AudioClip[]) - звуки для конкретного слоя
        - `Layer` (LayerMask) - слой поверхности

### `ImpactData`
- **Путь создания:** Create > Effects > ImpactData
- **Поля:**
    - `Impacts` (Impact[]) - массив эффектов попадания:
        - `Prefab` (ParticleView) - префаб частиц
        - `Mask` (LayerMask) - слой поверхности для эффекта

## 🔧 Порядок инициализации систем

Системы выполняются в следующем порядке:

1. **Init системы (однократно при старте):**
    - `EntityInjectionSystem` - регистрация сущностей
    - `ChildInjectionSystem` - инъекция дочерних объектов
    - `InventoryInitialSystem` - инициализация инвентаря
    - `InventoryFindNearestItemSystem.Init()` - инициализация поиска предметов
    - `InventoryEquipSystem.Init()` - скрытие неактивного оружия
    - `CharacterInjectWeaponSystem` - привязка оружия к рукам
    - `AudioSystem.Init()` - инициализация аудио

2. **Update системы (каждый кадр):**
    - `MouseRaycastSystem` - рейкаст мыши
    - `PlayerInputSystem` - ввод игрока
    - `PlayerDirectionSystem` - направление персонажа
    - `PlayerRiggingSystem` - IK наведение оружия
    - `InventoryFindNearestItemSystem` - поиск предметов
    - `InventoryPickupSystem` - подбор предметов
    - `InventoryHandlerSystem` - обработка инвентаря
    - `InventoryEquipSystem` - экипировка оружия
    - `InventoryDisplaySystem` - отображение инвентаря
    - `WeaponRaycastSystem` - рейкаст оружия
    - `WeaponLaserSystem` - визуализация луча
    - `WeaponShootSystem` - выстрел
    - `WeaponHitSystem` - обработка попаданий
    - `WeaponParticleSystem` - частицы выстрела
    - `WeaponSoundSystem` - звуки выстрела
    - `WeaponAnimatorSystem` - анимация оружия
    - `CharacterMotionSystem` - движение
    - `CharacterFootstepSystem` - звуки шагов
    - `CharacterAnimatorSystem` - анимация персонажа
    - `CharacterDamageSystem` - получение урона
    - `CharacterDestroySystem` - уничтожение сущностей
    - `AudioSystem` - воспроизведение звуков
    - `ParticleSystem` - управление частицами